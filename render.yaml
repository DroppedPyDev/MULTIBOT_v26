services:
  - type: worker
    name: telegram-bot
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: MAX_CONCURRENT_JOBS
        value: "3"
      - key: TEMP_DIR
        value: temp
